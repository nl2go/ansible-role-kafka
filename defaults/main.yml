---

kafka_user: kafka
kafka_group: kafka

kafka_version: 2.5.0
kafka_scala_version: 2.12
kafka_broker_id: 1

kafka_install_dir: /usr/local/kafka
kafka_conf_dir: /etc/kafka
kafka_log_dir: /var/log/kafka

kafka_zookeeper_connect_hosts:
  - localhost:2181
